/**
 * Incrementar quando o formato persistido ou o contrato exposto a workers/APIs mudar.
 * Runtime e UI podem usar isto para migrações e compatibilidade em deploys.
 */
export const AGENT_RUNTIME_CONTRACT_VERSION = 1;
